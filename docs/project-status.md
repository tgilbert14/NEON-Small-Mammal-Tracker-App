# Project status — NEON Small Mammal Tracker

> The context file the coordinators (`vgs`, `hk`, `neonize`) inject into every specialist brief so a
> cold-booted agent doesn't re-flag shipped or deliberately-deferred work. Owned by `triage`; keep it
> current after every review or ship. Depth: `README.md`, `DEPLOY.md`, `docs/neonize-playbook.md`.

**Last verified against the build and public endpoints:** 2026-07-18 (draft PR #73 release-recovery pass).

## Stage
- **P0 release recovery / public outage:** Pages landing works; Connect renders `Startup Error`.
- Draft PR #73 is the only release-recovery line. `main`, production data, the watched refresh branch,
  and unrelated idea branches remain untouched until evidence is complete.
- Still the **flagship / reference** app for honest-stats and product patterns, but not currently a
  healthy production reference.

## Shipped (don't re-flag as missing)
- National site-picker Leaflet map (by-site / by-species toggle, accessible list fallback, one-tap load).
- Per-site `.rds` bundles (instant load, no runtime network call); ~46 sites, 145 species.
- Detection-corrected abundance (Schnabel ≥3 nights / Chapman 2), MNKA, per-night detection, gated + clamped.
- Hill-number diversity profile; per-individual dossiers + holographic trading-card export; site compare; PDF report card.
- QC-flag system (ranked "verify, not wrong" flags + downloadable offending rows).
- Review-branch refresh CI: empty-stage producer, exact bundle/index/manifest/offline validator,
  restricted publisher, and semantic post-deploy outage issue.

## Implemented in draft PR #73 (not production-shipped)

- Cross-site detection-qualified compare, single-night/index-only coverage, tidy capture + monthly
  series exports, and a codebook are implemented in the current draft.
- The blank social image is replaced by a reviewed 1200×630 habitat card; the landing page no longer
  converts an opaque pre-warm request into a false readiness claim and has explicit focus treatment.
- Authority docs now agree on the outage, review-branch release boundary, pinned package provenance,
  and the ten-app suite. The in-app About panel links all nine companion products.

## Open / in-flight
- Complete the pinned R 4.5.2 CI ladder: actual eight-package geospatial closure, honest repository
  lanes, exact 46-site/schema/index/checksum gates, offline app source, and committed-manifest match.
- Commit only the manifest generated by that pinned validator; restore Connect and pass semantic smoke.
- Verify the revised landing at desktop/mobile breakpoints and the full Shiny UI after a restorable
  exact-head build becomes available; static responsive/reduced-motion/focus contracts remain gated.
- Long-term: Shinylive/WebAssembly static export (zero cold-start endgame) — see `DEPLOY.md` Option B.

## Deliberately deferred / non-goals
- Not a framework rewrite; not a multi-tenant app (it's a single public explorer).
- Live NEON API calls at runtime are intentionally avoided (bundled `.rds` is the design).

## P-items
- **P0:** close draft PR release gates and restore semantic production health.
- **P1:** run end-to-end Shiny interaction/mobile verification after a restorable exact-head build.
- **P2:** promote the reviewed Driver knowledge package only after 46-site parity and production health.
- **P3:** evaluate Shinylive only after the Connect release path is stable.
